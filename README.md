# Logger
A keylogger application written in pure python, designed to be as undetectable as possible with a special offline queue implementation that allows it to store keystrokes when the target is offline covering 100% of the target's actions regardless of connection availability
